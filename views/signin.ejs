<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Taskify - Sign In</title>
    <link rel="stylesheet" href="/auth/style.css">
</head>
<body>
    <div class="container">
        <div class="auth-card">
            <div class="logo">
                <img src="https://api.iconify.design/fluent:task-list-square-24-filled.svg?color=%23FF7A59" alt="Logo">
                <span class="logo-text">Taskify</span>
            </div>
            
            <div class="auth-header">
                <h1 class="auth-title">Welcome back</h1>
                <p class="auth-subtitle">Sign in to continue to your tasks</p>
            </div>

            <form onsubmit="event.preventDefault(); signin();">
                <div class="form-group">
                    <label class="form-label" for="email">Email address</label>
                    <input type="email" id="email" class="form-input email" placeholder="name@company.com" required>
                    <div class="error-message">Please enter a valid email address</div>
                </div>

                <div class="form-group">
                    <label class="form-label" for="password">Password</label>
                    <input type="password" id="password" class="form-input pswd" placeholder="••••••••" required>
                    <div class="error-message">Please enter your password</div>
                </div>

                <button type="submit" class="auth-button">Sign In</button>
            </form>

            <div class="auth-footer">
                Don't have an account? <a href="/" class="auth-link">Sign up</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="/auth/script.js"></script>
</body>
</html>