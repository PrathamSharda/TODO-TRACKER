<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Taskify - Your Tasks</title>
    <link rel="stylesheet" href="/todo/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="alert">
        <label class="alert-message"></label>
        <button onclick="hideAlert()" class="alert-btn">OK</button>
    </div>

    <div class="container">
        <header class="header">
            <div class="logo">
                <img src="https://api.iconify.design/fluent:task-list-square-24-filled.svg?color=%23FF7A59" alt="Logo">
                <span>Taskify</span>
            </div>
            <button onclick="removeCookie()" class="btn-logout">
                <i class="fas fa-sign-out-alt"></i>
                Sign Out
            </button>
        </header>

        <main class="main-content">
            <div class="task-input-container">
                <input type="text" class="task-input addingValue" placeholder="Add a new task...">
                <button onclick="addtask()" class="btn-add">Add Task</button>
            </div>

            <div class="board">
                <!-- Todo Column -->
                <div class="board-column">
                    <div class="column-header">
                        <h2 class="column-title todosub">TO DO</h2>
                        <div class="column-actions" style="display: none;">
                            <button onclick="completeTodo()" class="task-btn btn-complete">Complete</button>
                            <button onclick="deletetaskButton()" class="task-btn btn-delete">Delete</button>
                        </div>
                    </div>
                    <div class="task-list taskcreate">
                        <!-- Tasks will be dynamically added here -->
                    </div>
                </div>

                <!-- Completed Column -->
                <div class="board-column">
                    <div class="column-header">
                        <h2 class="column-title completesub">COMPLETED</h2>
                        <div class="column-actions" style="display: none;">
                            <button onclick="reverse()" class="task-btn btn-reverse">Reverse</button>
                            <button onclick="deleteCompButton()" class="task-btn btn-delete">Delete</button>
                        </div>
                    </div>
                    <div class="task-list listele">
                        <!-- Completed tasks will be dynamically added here -->
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="/todo/main.js"></script>
</body>
</html>


